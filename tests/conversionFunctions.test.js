var {
  toHSV,
  toHSL,
  toHex,
  toHex8,
  toRGB,
  toRGBA,
} = require('../lib/conversionFunctions');

describe('toHSV', () => {
  it('outputs a string', () => {});

  it('outputs in HSV format', () => {});

  it('returns null on invalid input', () => {});
});

describe('toHSL', () => {
  it('outputs a string', () => {});

  it('outputs in HSL format', () => {});

  it('returns null on invalid input', () => {});
});

describe('toHex', () => {
  it('outputs a string', () => {});

  it('outputs in Hex format', () => {});

  it('returns null on invalid input', () => {});
});

describe('toHex8', () => {
  it('outputs a string', () => {});

  it('outputs in Hex8 format', () => {});

  it('returns null on invalid input', () => {});
});

describe('toRGB', () => {
  it('outputs a string', () => {});

  it('outputs in RGB format', () => {});

  it('returns null on invalid input', () => {});
});

describe('toRGBA', () => {
  it('outputs a string', () => {});

  it('outputs in RGBA format', () => {});

  it('returns null on invalid input', () => {});
});

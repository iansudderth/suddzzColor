var { rgb, rgba, red, green, blue, mix } = require('../lib/RGBfunctions');

var {
  hsvFormat,
  hsvaFormat,
  hslFormat,
  hslaFormat,
  hexFormat,
  hex8Format,
  rgbFormat,
  rgbaFormat,
} = require('./formatRegEx');

describe('rgb', () => {
  it('returns a string', () => {});
});

describe('rgba', () => {
  it('returns a string', () => {});
});

describe('red', () => {
  it('returns a string', () => {});
});

describe('green', () => {
  it('returns a string', () => {});
});

describe('blue', () => {
  it('returns a string', () => {});
});

describe('mix', () => {
  it('returns a string', () => {});
});
